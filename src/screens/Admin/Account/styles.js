import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        marginTop: 90,
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center'
    },
    logoutButton: {
        borderWidth: 1,
        borderRadius: 10,
        marginVertical: 16
    }
})